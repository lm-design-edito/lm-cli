{
  "name": "@design-edito/cli",
  "version": "0.1.17",
  "description": "",
  "type": "module",
  "author": "Maxime Fabas",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/lm-design-edito/lm-cli"
  },
  "scripts": {
    "make:build": "mkdir -p build",
    "build:scripts": "npx tsc -p scripts/tsconfig.json",
    "exec:build": "node .scripts/build/index.js",
    "exec:pre-publish": "node .scripts/pre-publish/index.js",
    "build:src": "rm -rf build && npm run make:build && npm run exec:build",
    "build": "npm run build:scripts && npm run build:src",
    "prepublishOnly": "npm run build && npm run exec:pre-publish"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "@types/prompts": "^2.4.9",
    "@types/semver": "^7.8.0",
    "esbuild": "^0.28.2",
    "semver": "^7.7.4",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@design-edito/tools": "^0.5.0",
    "commander": "^15.0.0",
    "prompts": "^2.4.2",
    "simple-git": "^3.33.0"
  },
  "overrides": {
    "ws": ">8.17.0"
  }
}
